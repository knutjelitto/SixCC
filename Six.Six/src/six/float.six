namespace six;

shared builtin f32
{
}

shared builtin f64
{
    shared prefix f64 - () => builtin;
    shared infix f64 + (f64 rhs) => builtin;
    shared infix f64 - (f64 rhs) => builtin;
    shared infix f64 * (f64 rhs) => builtin;
    shared infix f64 / (f64 rhs) => builtin;
}

shared alias float => f32;
shared alias double => f64;
