namespace six.core;

shared native class Pointer : Builtin
{
    shared native new (uint raw);
    shared native uint getU32() => self.getU32();
    shared native Pointer getPointer() => self.getPointer();
    //shared native void setVTable(Pointer clazz) => self.setVTable(clazz);
}

