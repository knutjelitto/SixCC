namespace six;

shared builtin Boolean of true | false
{
    shared operator bool _|_ (bool rhs) => builtin;
    shared operator bool _&_ (bool rhs) => builtin;
    shared operator bool _^_ (bool rhs) => builtin;
    shared operator bool !_ () => builtin;
}

shared alias bool => Boolean;

shared Boolean true => builtin;
shared Boolean false => builtin;

