namespace six.core;

shared interface Identifiable
{
    shared abstract bool equals(Object that)
        ;/*TODO
        =>  if (is Identifiable that)
            then this === that
            else false;
        */
    
    shared virtual int hash => identityHash(self);

    shared static native int identityHash(Identifiable identifiable);
}
