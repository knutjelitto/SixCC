namespace six;

shared builtin Boolean
{
    shared infix bool | (bool rhs) => builtin;
    shared infix bool & (bool rhs) => builtin;
    shared infix bool ^ (bool rhs) => builtin;
    shared prefix bool ! () => builtin;
}

shared alias bool => Boolean;

shared bool true => builtin;
shared bool false => builtin;

