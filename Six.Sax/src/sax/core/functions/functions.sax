namespace sax.core;

"The identity function that always returns its argument."
tagged("Functions")
shared function Value identity<Value>(Value argument) => argument;

"A `void` function that does nothing."
tagged("Functions")
shared function void noop(Anything* arguments) {}
